plugin: amazon.aws.aws_ec2
regions:
  - us-east-1
filters:
  instance-state-name: running
  tag:Name: ansible-dynamic
hostnames:
  - public-ip-address
keyed_groups:
  - key: tags.Name
    prefix: tag
    separator: '-'
